ethernet:
  type: LAN8720
  mdc_pin: GPIO23
  mdio_pin: GPIO18
  clk_mode: GPIO0_IN
  phy_addr: 0
  domain: !secret wifi-domain

text_sensor:
  - platform: ethernet_info
    ip_address:
      name: ESP IP Address
